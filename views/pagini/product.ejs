<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="keywords" content="sparkle, sprkl, gaming store, games, buy games, online gaming shop, cool games, epic games">
    <meta name="description" content="Sparkle(SPRKL) is the safest and fastest way of buying games.">
    <meta name="author" content="Vladimir Stratulat">
    <title>SPARKLE</title>
    
    <%- include("../fragmente/head.ejs") %>
</head>
<body>
    <%- include("../fragmente/header.ejs") %>
    <main>
        <article class="product product-productpage">
            <div class="info-produse info-produse-productpage">
                <a href="/product/<%- product.product_id %>"><span class="name"><%- product.name%></span></a>
                <p class="price">Price: <span class="val-price">
                    <% if (product.price != 0) {%>
                    <%- product.price %>
                    <% } else { %>
                        FREE <% } %>
                    </span></p>
                <p class="category">Category: <span class="val-category"><%- product.category %></span></p>
                <p class="description">Description: <span class="val-description"><%- product.description %></span></p>
                <p class="age-restriction">PEGI: <span class="val-age-restriction"><%- product.age_restriction %></span></p>
                <p class="genres">Genres: <span class="val-genres"><%- product.genres %></span></p>
                <p class="publisher">Publisher: <span class="val-publisher"><%- product.publisher %></span></p>
                <p class="copies">Physical Copies: <span class="val-copies"><% if(product.physical_copies) { %> Available <% } else { %> Not Available <%}%></span></p>
            </div>
            <figure> 
                <a href="/product/<%- product.product_id %>"> 
                   <% if(product.image === null) {
                    let imgName = "imageNull.png"; %>
                    <img src="/resurse/imagini/products_pics/<%-imgName%>" alt="product-image">
                    <% } else { %>
                    <img src="/resurse/imagini/products_pics/<%- product.image %>" alt="product-image">
                    <% } %>
                    </a>
            </figure>
        </article>
    </main>
    <br><br>
    <%- include("../fragmente/footer.ejs") %>
</body>
</html>